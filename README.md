# docker-discovery-dns

Proof of concept project that implements DNS SRV service discovery for local docker server. Every time DNS request comming it ask docker server for running and healthy containers.
